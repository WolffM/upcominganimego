import { 
  SeasonInfo, 
  FilterOptions, 
  SortOption, 
  ANIME_FORMATS, 
  MAX_REASONABLE_PAGES 
} from '@/types/anime';

// Re-export the types for backward compatibility
export { ANIME_FORMATS, MAX_REASONABLE_PAGES };
export type { SeasonInfo, FilterOptions, SortOption }; 